{
  "name": "daftar-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Daftar Portfolio Manager for Movement Network",
  "type": "module",
  "scripts": {
    "setup": "node scripts/setup.js",
    "start": "npm run start --workspace=apps/server",
    "dev": "concurrently -n \"server,worker,frontend\" -c \"magenta,yellow,cyan\" \"npm run dev --workspace=apps/server\" \"npm run worker --workspace=apps/server\" \"npm run dev --workspace=apps/frontend\"",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.9.1",
    "concurrently": "^9.2.1",
    "typescript": "^5.0.0"
  }
}
